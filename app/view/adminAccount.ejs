<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, sans-serif;
      }

      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f4f4f4;
      }

      .container {
        width: 80%;
        max-width: 800px;
        background: white;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      }

      .admin-info {
        text-align: center;
        margin-bottom: 20px;
      }

      .admin-info h2 {
        margin-bottom: 5px;
      }

      .search-box {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
      }

      .search-box input {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }

      .user-list {
        list-style: none;
      }

      .user-list li {
        background: #e3e3e3;
        padding: 10px;
        margin-bottom: 5px;
        border-radius: 5px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="admin-info">
        <h2>Admin:</h2>
      </div>

      <!-- Formulaire de recherche -->
      <div class="search-box">
        <form method="GET" action="/adminAccount">
          <input
            type="text"
            name="searchTerm"
            value="<%= searchTerm %>"
            placeholder="Rechercher un utilisateur..."
          />
          <button type="submit">Rechercher</button>
        </form>
      </div>

      <!-- Liste des utilisateurs -->
      <ul class="user-list">
        <% if (users.length > 0) { %> <% users.forEach(function(user) { %>
        <li><%= user.username %></li>
        <% }); %> <% } else { %>
        <li>Aucun utilisateur trouvé.</li>
        <% } %>
      </ul>

      <!-- Bouton de déconnexion -->
      <div class="logout-btn">
        <a href="/adminAccount/logout">Déconnexion</a>
      </div>
    </div>
  </body>
</html>
